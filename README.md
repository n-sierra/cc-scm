# cc-scm

cc-scm is a scheme interpreter, which is implemented by Lua.

## Usage

    $ lua repl.lua
    LUASCHEME INTERPRETER
    scm> (+ 1 2)
    3
    scm> ((lambda (x) (* x x)) 16)
    256
    scm>
